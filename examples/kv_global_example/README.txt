kv_global_example — Demonstrates kv::global for contract-wide configuration.

Features shown:
- set / get / exists / get_or_default
- Asserting get with custom message
- Modify-and-set pattern (read, modify, write back)
- Trivially-copyable struct (zero-copy serialization)
