{
   "tests": [
      {
         "compile_flags": ["--abigen"],
         "expected": {
            "stderr": "is not a usable table name; use only letters, digits and underscore"
         }
      }
   ]
}
